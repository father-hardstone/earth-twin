import { t as getBasePath } from "./chunk.3Y6SB6QS-D4zZaEoT.js";
//#region node_modules/@shoelace-style/shoelace/dist/shoelace-autoloader.js
var observer = new MutationObserver((mutations) => {
	for (const { addedNodes } of mutations) for (const node of addedNodes) if (node.nodeType === Node.ELEMENT_NODE) discover(node);
});
async function discover(root) {
	const rootTagName = root instanceof Element ? root.tagName.toLowerCase() : "";
	const rootIsShoelaceElement = rootTagName == null ? void 0 : rootTagName.startsWith("sl-");
	const tags = [...root.querySelectorAll(":not(:defined)")].map((el) => el.tagName.toLowerCase()).filter((tag) => tag.startsWith("sl-"));
	if (rootIsShoelaceElement && !customElements.get(rootTagName)) tags.push(rootTagName);
	const tagsToRegister = [...new Set(tags)];
	await Promise.allSettled(tagsToRegister.map((tagName) => register(tagName)));
}
function register(tagName) {
	if (customElements.get(tagName)) return Promise.resolve();
	const tagWithoutPrefix = tagName.replace(/^sl-/i, "");
	const path = getBasePath(`components/${tagWithoutPrefix}/${tagWithoutPrefix}.js`);
	return new Promise((resolve, reject) => {
		import(path).then(() => resolve()).catch(() => reject(/* @__PURE__ */ new Error(`Unable to autoload <${tagName}> from ${path}`)));
	});
}
discover(document.body);
observer.observe(document.documentElement, {
	subtree: true,
	childList: true
});
//#endregion
export { discover };

//# sourceMappingURL=@shoelace-style_shoelace_dist_shoelace-autoloader__js.js.map